// ============================================
// Подключаем библиотеку для работы с вводом-выводом (печать на экран, чтение с клавиатуры)
// stdio.h = Standard Input Output header (заголовочный файл стандартного ввода-вывода)
// ============================================
#include <stdio.h>

// ============================================
// ОБЪЯВЛЕНИЕ ФУНКЦИЙ
// Это как "оглавление" - говорим компилятору, что эти функции существуют ниже в коде
// int перед названием = функция возвращает целое число
// void перед названием = функция ничего не возвращает (просто выполняет действие)
// ============================================

// Функция рисования всего игрового поля целиком
// Принимает размеры поля, позиции ракеток, позицию мяча и счёт
int PrintPole(int width,           // ширина поля (80 символов)
              int height,          // высота поля (25 строк)
              int YLeftRocket,     // позиция левой ракетки по вертикали (номер строки)
              int XLeftRocket,     // позиция левой ракетки по горизонтали (номер столбца)
              int YRightRocket,    // позиция правой ракетки по вертикали
              int XRightRocket,    // позиция правой ракетки по горизонтали
              int XBall,           // позиция мяча по горизонтали
              int YBall,           // позиция мяча по вертикали
              int GoalLeft,        // счёт левого (синего) игрока
              int ScoreRight);     // счёт правого (красного) игрока
// ; точка с запятой означает "объявление функции закончено"

// Функция рисования левой ракетки (синий символ ▓)
void PrintLeftRocket();
// ; конец объявления

// Функция рисования правой ракетки (красный символ ▓)
void PrintRightRocket();
// ; конец объявления

// Функция рисования сетки посередине поля (символ ░)
void PrintWeb();
// ; конец объявления

// Функция рисования левой границы и сетки (не используется в этой версии)
void PrintLeftBorderAndWeb(int width);
// ; конец объявления

// Функция рисования правой границы (не используется в этой версии)
void PrintRightBorder(int width);
// ; конец объявления

// Функция рисования левого верхнего угла рамки (символ ┌)
void LeftTopAngle();
// ; конец объявления

// Функция рисования правого верхнего угла рамки (символ ┐)
void RightTopAngle();
// ; конец объявления

// Функция рисования левого нижнего угла рамки (символ └)
void LeftBottomAngle();
// ; конец объявления

// Функция рисования правого нижнего угла рамки (символ ┘)
void RightBottomAngle();
// ; конец объявления

// Функция рисования горизонтальной линии (символы ───)
void HorizontalBorder(int width);  // принимает ширину линии
// ; конец объявления

// Функция рисования правой границы после ракетки (не используется)
void PrintRightBorderAfterRocket();
// ; конец объявления

// Функция рисования нижней границы поля (нижняя рамка)
void PrintBottomBorder();
// ; конец объявления

// Функция рисования верхней границы поля (верхняя рамка)
void PrintTopBorder();
// ; конец объявления

// Функция рисования мяча (жёлтый символ ●)
void PrintBall();
// ; конец объявления

// Функция рисования вертикальной границы (боковая стенка │)
void PrintVerticalBorder();
// ; конец объявления

// Функция вывода счёта игры на экран
void PrintScore(int ScreenX,       // ширина экрана (для центрирования)
                int GoalLeft,      // счёт левого игрока
                int GoalRight);    // счёт правого игрока
// ; конец объявления

// ============================================
// ГЛАВНАЯ ФУНКЦИЯ ПРОГРАММЫ
// Программа всегда начинает выполнение с функции main()
// int main() означает, что функция вернёт целое число (код завершения)
// ============================================
int main() {
// { открывающая фигурная скобка - начало тела функции main
// Весь код между { и } относится к функции main

    // ============================================
    // БЛОК 1: ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ ДЛЯ ИГРЫ
    // Создаём "коробки" для хранения информации
    // int = тип данных "целое число"
    // ============================================
    
    // Позиции ракеток по вертикали (номер строки на экране)
    int YLeftRocket = 12,          // левая ракетка на строке 12 (середина поля)
    // int - тип данных "целое число"
    // YLeftRocket - название переменной (Y = вертикаль, Left = левая, Rocket = ракетка)
    // = оператор присваивания
    // 12 - начальное значение (середина поля высотой 25 строк)
    // , запятая означает "объявляем ещё одну переменную того же типа"
    
        YRightRocket = 12;         // правая ракетка тоже на строке 12 (середина)
    // YRightRocket - правая ракетка
    // = 12 - тоже в середине
    // ; точка с запятой означает "команда закончена"
    // Комментарий в коде: координаты платформ по вертикали Y = 12 для обеих, чтобы отобразить посередине
    
    // Позиция мяча (где находится мяч на поле)
    int XBall = 3,                 // мяч в столбце 3 (у левого края, рядом с левой ракеткой)
    // XBall - позиция мяча по горизонтали (X = горизонталь)
    // = 3 - в столбце 3
    // , запятая - объявляем следующую переменную
    
        YBall = 13;                // мяч на строке 13 (чуть ниже середины)
    // YBall - позиция мяча по вертикали (Y = вертикаль)
    // = 13 - на строке 13
    // ; конец команды
    // Комментарий:  координаты мячика, мяч у левого игрока
    
    // Скорость мяча (на сколько клеток двигается за один ход)
    // Это одновременно и направление движения
    int speedX = 1,                // скорость по горизонтали:  1 = вправо, -1 = влево
    // speedX - скорость/направление по оси X (горизонталь)
    // = 1 - движение на 1 клетку вправо за ход
    // , запятая
    
        speedY = 1;                // скорость по вертикали:  1 = вниз, -1 = вверх
    // speedY - скорость/направление по оси Y (вертикаль)
    // = 1 - движение на 1 клетку вниз за ход
    // ; конец команды
    // Комментарий: скорость перемещения мячика 2 символа за ход (на самом деле по 1 на каждую ось)
    
    // Флаг хода (чья очередь ходить)
    int Player = 0;                // 0 = ходит левый игрок, 1 = ходит правый игрок
    // Player - переменная, хранящая номер текущего игрока
    // = 0 - начинает левый (синий) игрок
    // ; конец команды
    // Комментарий: флаг хода:  0 Если ход левого игрока, 1 ход правого игрока
    
    // Счёт игры (количество забитых голов каждым игроком)
    int GoalLeft = 0,              // счёт левого игрока (начинаем с 0)
    // GoalLeft - голы левого игрока
    // = 0 - в начале игры 0 голов
    // , запятая
    
        GoalRight = 0;             // счёт правого игрока (начинаем с 0)
    // GoalRight - голы правого игрока
    // = 0 - в начале игры 0 голов
    // ; конец команды
    // Комментарий: количество голов для левого и правого игрока
    
    // Размер игрового поля (сколько символов в ширину и строк в высоту)
    int ScreenX = 80,              // ширина поля:  80 символов
    // ScreenX - ширина экрана/поля (X = горизонталь)
    // = 80 - поле шириной 80 символов
    // , запятая
    
        ScreenY = 25;              // высота поля:  25 строк
    // ScreenY - высота экрана/поля (Y = вертикаль)
    // = 25 - поле высотой 25 строк
    // ; конец команды
    // Комментарий:  размер игровой площадки
    
    // Позиции ракеток по горизонтали (в каком столбце находятся)
    // Ракетки не двигаются влево-вправо, только вверх-вниз
    int XLeftRocket = 2,           // левая ракетка в столбце 2 (у левого края)
    // XLeftRocket - позиция левой ракетки по горизонтали
    // = 2 - в столбце 2 (близко к левому краю)
    // , запятая
    
        XRightRocket = ScreenX - 3;// правая ракетка в столбце 77 (80 - 3 = 77, у правого края)
    // XRightRocket - позиция правой ракетки по горизонтали
    // = ScreenX - 3 - вычисляем:  80 - 3 = 77 (близко к правому краю)
    // - оператор вычитания
    // ; конец команды

    // ============================================
    // БЛОК 2: НАЧАЛЬНАЯ ОТРИСОВКА ИГРЫ
    // Выводим счёт и рисуем поле перед началом игры
    // ============================================
    
    // Выводим счёт на экран (в начале 0 :  0)
    PrintScore(ScreenX, GoalLeft, GoalRight);
    // PrintScore - название функции вывода счёта
    // ( открывающая круглая скобка - начало списка параметров
    // ScreenX - ширина экрана (нужна для центрирования счёта)
    // , запятая разделяет параметры
    // GoalLeft - счёт левого игрока (0)
    // , запятая
    // GoalRight - счёт правого игрока (0)
    // ) закрывающая круглая скобка - конец списка параметров
    // ; точка с запятой - конец команды
    
    // Рисуем всё игровое поле:  рамку, ракетки, мяч, сетку
    PrintPole(ScreenX,             // ширина поля (80)
              ScreenY,             // высота поля (25)
              YLeftRocket,         // позиция левой ракетки по вертикали (12)
              XLeftRocket,         // позиция левой ракетки по горизонтали (2)
              YRightRocket,        // позиция правой ракетки по вертикали (12)
              XRightRocket,        // позиция правой ракетки по горизонтали (77)
              XBall,               // позиция мяча по горизонтали (3)
              YBall,               // позиция мяча по вертикали (13)
              GoalLeft,            // счёт левого игрока (0)
              GoalRight);          // счёт правого игрока (0)
    // PrintPole - функция рисования поля
    // Передаём все переменные с позициями и счётом
    // ; конец команды
    // Комментарий в коде: функция рисует игровое поле в начальном состоянии
    
    // Выводим подсказку, чей ход (в начале ходит синий)
    printf("    \033[1m!!  Ход \x1b[36m синего \x1b[0m игрока ! !\033[0m \n");
    // printf - функция печати текста на экран
    // ( открывающая скобка параметров
    // " открывающая кавычка - начало текстовой строки
    //     - 4 пробела для отступа слева
    // \033[1m - ANSI escape-код:  делает текст жирным
    // !!  Ход  - текст
    // \x1b[36m - ANSI escape-код: делает текст голубым
    //  синего  - это слово будет голубым
    // \x1b[0m - ANSI escape-код: сбрасывает все цвета и стили (возврат к обычному)
    //  игрока !! - остальной текст
    // \033[0m - ещё раз сброс стилей (для надёжности)
    //   - пробел
    // \n - символ переноса строки (переход на новую строку)
    // " закрывающая кавычка - конец текстовой строки
    // ) закрывающая скобка параметров
    // ; конец команды

    // ============================================
    // БЛОК 3: ПОДГОТОВКА К ИГРОВОМУ ЦИКЛУ
    // ============================================
    
    // Переменная для хранения нажатой клавиши
    char input;
    // char - тип данных "символ" (может хранить одну букву, цифру или знак)
    // input - название переменной
    // ; конец команды
    
    // ============================================
    // БЛОК 4: ГЛАВНЫЙ ИГРОВОЙ ЦИКЛ
    // Бесконечный цикл - игра идёт, пока игрок не нажмёт 'q' или кто-то не наберёт 21 очко
    // ============================================
    
    while (1) {                    // запускаем бесконечный цикл
    // while - ключевое слово "пока" (начало цикла)
    // ( открывающая скобка условия
    // 1 - условие цикла (1 = истина, условие всегда верно, поэтому цикл бесконечный)
    // ) закрывающая скобка условия
    // { открывающая фигурная скобка - начало тела цикла
    // Весь код между этой { и соответствующей } будет повторяться бесконечно
    // Комментарий:  Запускаем бесконечный цикл
    
        // Ждём нажатия клавиши от игрока
        input = getchar();         // считываем один символ с клавиатуры
        // input - переменная, куда сохраняем результат
        // = оператор присваивания
        // getchar() - функция чтения одного символа с клавиатуры
        // () пустые скобки - функция не принимает параметров
        // Функция ОСТАНАВЛИВАЕТ программу и ждёт, пока игрок нажмёт клавишу
        // ; конец команды
        // Комментарий: ожидаем ввод с клавиатуры, считываем символ

        // ============================================
        // БЛОК 5: ПРОВЕРКА ГОЛОВ
        // Проверяем, не ушёл ли мяч за границы поля
        // ============================================
        
        // Проверяем:  мяч ушёл за левую границу? 
        if (XBall <= 0) {          // если мяч в столбце 0 или левее
        // if - ключевое слово "если"
        // ( открывающая скобка условия
        // XBall <= 0 - условие:  "позиция мяча по X меньше или равна 0"
        // <= оператор "меньше или равно"
        // ) закрывающая скобка условия
        // { открывающая фигурная скобка - начало блока if
        // Код внутри { } выполнится ТОЛЬКО если условие истинно (мяч ушёл за левый край)
        // Комментарий: мяч вышел за левую границу, гол синему игроку, сбрасываем все значения на начальные
        
            // Увеличиваем счёт левого игрока на 1
            GoalLeft++;            // было 0, стало 1 (или было 5, стало 6)
            // GoalLeft - переменная счёта левого игрока
            // ++ - оператор инкремента:  увеличивает значение на 1
            // GoalLeft++ это сокращённая запись от GoalLeft = GoalLeft + 1
            // ; конец команды
            // Комментарий: добавляем очко левому игроку
            
            // Выводим сообщение о голе с отступом для центрирования
            printf("%*s%sГол \x1b[36m синему \x1b[0m  игроку\n ", ScreenX / 2 - 17, " ", " ");
            // printf - функция печати
            // ( открывающая скобка параметров
            // " открывающая кавычка строки
            // %*s - спецификатор формата:  вставить строку (*) с динамической шириной
            // %s - спецификатор формата: вставить строку
            // Гол  - текст сообщения
            // \x1b[36m - ANSI код:  голубой цвет
            //  синему  - это слово будет голубым
            // \x1b[0m - сброс цвета
            //   игроку - остальной текст
            // \n - новая строка
            //   - пробел
            // " закрывающая кавычка строки
            // , запятая разделяет параметры
            // ScreenX / 2 - 17 - вычисляем отступ:  половина ширины экрана минус 17
            // / оператор деления (80 / 2 = 40, 40 - 17 = 23)
            // , запятая
            // " " - строка из одного пробела (для первого %*s)
            // , запятая
            // " " - строка из одного пробела (для второго %s)
            // ) закрывающая скобка параметров
            // ; конец команды
            // Комментарий:  Сообщение Гол
            
            // Сбрасываем позицию левой ракетки в начальное состояние
            YLeftRocket = 12;      // ставим ракетку на строку 12 (середина)
            // YLeftRocket - позиция левой ракетки по вертикали
            // = оператор присваивания
            // 12 - новое значение (середина поля)
            // ; конец команды
            // Комментарий: положение сверху левой ракетки
            
            // Сбрасываем позицию правой ракетки
            YRightRocket = 12;     // ставим ракетку на строку 12 (середина)
            // = присваивание
            // 12 - середина
            // ; конец команды
            // Комментарий:  положение сверху правой ракетки
            
            // Сбрасываем горизонтальную позицию левой ракетки
            XLeftRocket = 2;       // ставим в столбец 2 (у левого края)
            // = присваивание
            // 2 - позиция у левого края
            // ; конец команды
            // Комментарий: положение слева левой ракетки
            
            // Сбрасываем горизонтальную позицию правой ракетки
            XRightRocket = ScreenX - 3; // ставим в столбец 77 (у правого края)
            // = присваивание
            // ScreenX - 3 = 80 - 3 = 77
            // ; конец команды
            // Комментарий: положение слева правой ракетки
            
            // Сбрасываем горизонтальную позицию мяча
            XBall = 3;             // мяч в столбце 3 (у левой ракетки)
            // = присваивание
            // 3 - рядом с левой ракеткой
            // ; конец команды
            // Комментарий: начальное положение мяча в центре левой ракетки
            
            // Сбрасываем вертикальную позицию мяча
            YBall = 13;            // мяч на строке 13
            // = присваивание
            // 13 - чуть ниже середины
            // ; конец команды
            // Комментарий: начальное положение мяча в центре левой ракетки
            
            // Сбрасываем скорость мяча по горизонтали
            speedX = 1;            // мяч полетит вправо
            // = присваивание
            // 1 - движение вправо (на 1 клетку за ход)
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси x
            
            // Сбрасываем скорость мяча по вертикали
            speedY = 1;            // мяч полетит вниз
            // = присваивание
            // 1 - движение вниз (на 1 клетку за ход)
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси y
            
            // Устанавливаем, что начинает левый игрок
            Player = 0;            // ход левого игрока
            // = присваивание
            // 0 - левый игрок
            // ; конец команды
            // Комментарий:  Игрок начинающий ход
            
            // Пропускаем весь остальной код в цикле и начинаем новый круг
            continue;
            // continue - команда "перейти к началу цикла"
            // Программа вернётся к строке while (1) и снова будет ждать ввода
            // ; конец команды
            // Комментарий: гол левому новый раунд

        }
        // } закрывающая фигурная скобка - конец блока if (XBall <= 0)
        // Здесь заканчивается код, который выполняется при голе левому игроку
        
        // Проверяем другое условие: мяч ушёл за правую границу? 
        else if (XBall >= ScreenX - 1) { // если мяч в столбце 79 или правее
        // else - ключевое слово "иначе"
        // if - ключевое слово "если"
        // ( открывающая скобка условия
        // XBall >= ScreenX - 1 - "позиция мяча больше или равна 79"
        // >= оператор "больше или равно"
        // ScreenX - 1 = 80 - 1 = 79 (правая граница поля)
        // ) закрывающая скобка условия
        // { открывающая фигурная скобка - начало блока else if
        // Этот код выполнится, если первое условие (XBall <= 0) было ложно,
        // но второе условие (XBall >= 79) истинно
        
            // Увеличиваем счёт правого игрока на 1
            GoalRight++;           // было 0, стало 1
            // GoalRight - счёт правого игрока
            // ++ увеличить на 1
            // ; конец команды
            // Комментарий: добавляем очко правому игроку
            
            // Выводим сообщение о голе красному игроку
            printf("%*s%sГол \x1b[32m красному \x1b[0m  игроку\n ", ScreenX / 2 - 17, " ", " ");
            // printf - печать
            // %*s, %s - места для вставки строк
            // Гол  - текст
            // \x1b[32m - зелёный цвет (используем вместо красного)
            //  красному  - это слово будет зелёным
            // \x1b[0m - сброс цвета
            // ScreenX / 2 - 17 - отступ для центрирования
            // ; конец команды
            // Комментарий: Сообщение Гол
            
            // Сбрасываем позиции (аналогично голу левому)
            YLeftRocket = 12;      // левая ракетка в середину
            // ; конец команды
            // Комментарий: положение сверху левой ракетки
            
            YRightRocket = 12;     // правая ракетка в середину
            // ; конец команды
            // Комментарий: положение сверху правой ракетки
            
            XLeftRocket = 2;       // левая ракетка к левому краю
            // ; конец команды
            // Комментарий: положение слева левой ракетки
            
            XRightRocket = ScreenX - 3; // правая ракетка к правому краю
            // ; конец команды
            // Комментарий: положение слева правой ракетки
            
            // ОТЛИЧИЕ: мяч стартует у ПРАВОГО игрока
            XBall = XRightRocket - 2;   // мяч рядом с правой ракеткой (77 - 2 = 75)
            // XRightRocket - 2 = 77 - 2 = 75
            // ; конец команды
            // Комментарий: начальное положение мяча в центре правой ракетки
            
            YBall = 13;            // мяч на строке 13
            // ; конец команды
            // Комментарий: начальное положение мяча в центре правой ракетки
            
            // ОТЛИЧИЕ: мяч полетит ВЛЕВО
            speedX = -1;           // -1 означает движение влево
            // = присваивание
            // -1 - отрицательное число, движение влево
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси x
            
            // ОТЛИЧИЕ:  мяч полетит ВВЕРХ
            speedY = -1;           // -1 означает движение вверх
            // = присваивание
            // -1 - отрицательное число, движение вверх
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси y
            
            // ОТЛИЧИЕ: начинает правый игрок
            Player = 1;            // 1 = правый игрок
            // = присваивание
            // 1 - правый игрок
            // ; конец команды
            // Комментарий: Игрок начинающий ход
            
            // Переходим к началу цикла (начинаем новый раунд)
            continue;
            // continue - пропустить остальной код
            // ; конец команды
            // Комментарий: гол правому игроку, начинаем новый раунд
            
        }
        // } закрывающая фигурная скобка - конец блока else if
        // Здесь заканчивается обработка гола правому игроку

        // ============================================
        // БЛОК 6: ПРОВЕРКА ПОБЕДЫ
        // Проверяем, не набрал ли кто-то 21 очко
        // ============================================
        
        // Проверяем: левый игрок набрал 21 очко? 
        if (GoalLeft == 21) {      // если счёт левого равен 21
        // if - "если"
        // ( открывающая скобка условия
        // GoalLeft == 21 - "счёт левого игрока равен 21"
        // == оператор проверки на равенство (НЕ путать с = присваиванием!)
        // ) закрывающая скобка условия
        // { начало блока if
        // Комментарий: проверка на счёт
        
            // Выводим сообщение о победе синего игрока
            printf("     Синий игрок попедил со счётом \x1b[34m%d\x1b[0m :  \x1b[31m%d\x1b[0m\n", GoalLeft, GoalRight);
            // printf - печать
            // "      - начало строки с 5 пробелами
            // Синий игрок победил со счётом  - текст
            // \x1b[34m - синий цвет
            // %d - место для вставки целого числа (decimal = десятичное)
            // \x1b[0m - сброс цвета
            //  :   - двоеточие с пробелами
            // \x1b[31m - красный цвет
            // %d - место для второго числа
            // \x1b[0m - сброс цвета
            // \n - новая строка
            // " закрывающая кавычка
            // , запятая
            // GoalLeft - это число подставится вместо первого %d
            // , запятая
            // GoalRight - это число подставится вместо второго %d
            // ) закрывающая скобка
            // ; конец команды
            
            // Завершаем программу
            return 0;
            // return - команда "вернуть значение и выйти из функции"
            // 0 - код успешного завершения программы
            // Когда main завершается, вся программа закрывается
            // ; конец команды
            
        }
        // } конец блока if
        // Здесь заканчивается проверка победы левого игрока
        
        // Проверяем:  правый игрок набрал 21 очко?
        if (GoalRight == 21) {     // если счёт правого равен 21
        // if - "если"
        // ( открывающая скобка условия
        // GoalRight == 21 - "счёт правого игрока равен 21"
        // ) закрывающая скобка условия
        // { начало блока if
        
            // Выводим сообщение о победе красного игрока
            printf("     Красный игрок попедил со счётом \x1b[34m%d\x1b[0m : \x1b[31m%d\x1b[0m\n", GoalLeft, GoalRight);
            // Красный игрок победил - текст
            // Остальное аналогично предыдущему printf
            // ; конец команды
            
            // Завершаем программу
            return 0;
            // return 0 - выход из программы
            // ; конец команды
            
        }
        // } конец блока if
        // Здесь заканчивается проверка победы правого игрока

        // ============================================
        // БЛОК 7: ОБРАБОТКА НАЖАТЫХ КЛАВИШ
        // Проверяем, какую клавишу нажал игрок, и выполняем соответствующее действие
        // ============================================
        
        // Конструкция множественного выбора:  проверяем значение переменной input
        switch (input) {           // проверяем, какой символ в переменной input
        // switch - ключевое слово "переключатель"
        // ( открывающая скобка
        // input - переменная, значение которой проверяем
        // ) закрывающая скобка
        // { открывающая фигурная скобка - начало тела switch
        // Комментарий: проверяем какой символ введён
        
            // Вариант:  если нажата клавиша 'q'
            case 'q':              // если input равен символу 'q'
            // case - ключевое слово "вариант"
            // 'q' - символ в одинарных кавычках (одна буква)
            // : двоеточие означает "начало кода для этого варианта"
            // Комментарий: если введена q заканчиваем игру
            
                // Выводим сообщение о завершении
                printf("Игра закончена");
                // printf - печать на экран
                // "Игра закончена" - текст сообщения
                // ; конец команды
                
                // Завершаем программу
                return 0;
                // return 0 - выход из main, программа закрывается
                // ; конец команды
                // После return код дальше не выполняется
                
            // Вариант: если нажата клавиша Enter
            case '\n':             // если input равен символу переноса строки
            // case - "вариант"
            // '\n' - специальный символ "перенос строки" (клавиша Enter)
            // : начало кода
            // Комментарий:  игнорируем перевод строки клавиша ENTER
            
                // Ничего не делаем, просто выходим из switch
                break;
                // break - команда "выйти из switch, продолжить выполнение дальше"
                // ; конец команды
                
            // Вариант: все остальные клавиши
            default:               // если ни один case не подошёл
            // default - ключевое слово "по умолчанию" (все остальные случаи)
            // : начало кода
            // Комментарий: (пустой комментарий)
            
                // ============================================
                // БЛОК 8: ОПРЕДЕЛЯЕМ, ЧЕЙ ХОД
                // ============================================
                
                // Проверяем:  сейчас ходит левый игрок? 
                if (Player == 0) { // если Player равен 0 (левый игрок)
                // if - "если"
                // ( открывающая скобка условия
                // Player == 0 - "переменная Player равна 0"
                // == проверка на равенство
                // ) закрывающая скобка условия
                // { начало блока if
                // Комментарий: проверяем чей ход и в зависимости от нажатой клавиши устанавливаем координаты платформы

                    // ============================================
                    // БЛОК 9: ОБРАБОТКА НАЖАТИЙ ЛЕВОГО ИГРОКА
                    // ============================================
                    
                    // Ещё один switch:  проверяем, какую клавишу нажал левый игрок
                    switch (input) { // проверяем символ в input
                    // switch - "переключатель"
                    // ( открывающая скобка
                    // input - проверяемая переменная
                    // ) закрывающая скобка
                    // { начало switch
                    // Комментарий: ещё раз проверяем какой символ введён
                    
                        // Вариант: нажата клавиша 'a'
                        case 'a':  // если input равен 'a'
                        // case 'a' - вариант "буква a"
                        // :  начало кода
                        // Комментарий: движение левой ракетки вверх
                        
                            // Двигаем левую ракетку ВВЕРХ (уменьшаем номер строки)
                            YLeftRocket -= 1;
                            // YLeftRocket - позиция ракетки по вертикали
                            // -= оператор "вычесть из переменной"
                            // -= 1 это сокращение от YLeftRocket = YLeftRocket - 1
                            // Было: YLeftRocket = 12, стало: YLeftRocket = 11
                            // Меньший номер строки = выше на экране = движение вверх
                            // ; конец команды
                            
                            // Проверяем:  не вышла ли ракетка за верхнюю границу?
                            if (YLeftRocket <= 0) { // если позиция 0 или меньше
                            // if - "если"
                            // ( открывающая скобка условия
                            // YLeftRocket <= 0 - "позиция меньше или равна 0"
                            // <= "меньше или равно"
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим ракетку на строку 1 (не даём выйти за границу)
                                YLeftRocket = 1;
                                // = присваивание
                                // 1 - минимально возможная позиция (самая верхняя строка поля)
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            // Здесь заканчивается проверка границы
                            
                            // Передаём ход правому игроку
                            Player = 1;
                            // Player - переменная хода
                            // = присваивание
                            // 1 - теперь ходит правый игрок
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch (прекращаем проверку других case)
                            break;
                            // break - выход из switch
                            // ; конец команды
                            
                        // Вариант: нажата клавиша 'z'
                        case 'z':  // если input равен 'z'
                        // case 'z' - вариант "буква z"
                        // : начало кода
                        // Комментарий:  движение левой ракетки вниз
                        
                            // Двигаем левую ракетку ВНИЗ (увеличиваем номер строки)
                            YLeftRocket += 1;
                            // += оператор "прибавить к переменной"
                            // += 1 это сокращение от YLeftRocket = YLeftRocket + 1
                            // Было: YLeftRocket = 12, стало: YLeftRocket = 13
                            // Больший номер строки = ниже на экране = движение вниз
                            // ; конец команды
                            
                            // Проверяем: не вышла ли ракетка за нижнюю границу?
                            if (YLeftRocket >= ScreenX) { // если позиция 80 или больше
                            // if - "если"
                            // ( открывающая скобка условия
                            // YLeftRocket >= ScreenX - "позиция больше или равна 80"
                            // >= "больше или равно"
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим ракетку на строку ScreenX-3 (не даём выйти за поле)
                                YLeftRocket = ScreenX - 3;
                                // ScreenX - 3 = 80 - 3 = 77
                                // Ракетка занимает 3 строки, поэтому вычитаем 3
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход правому игроку
                            Player = 1;
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант:  левый игрок нажал 'k' (это кнопка правого игрока!)
                        case 'k':  
                        // case 'k' - вариант "буква k"
                        // : начало кода
                        
                            // Выводим предупреждение (напоминаем, что сейчас ход левого)
                            printf("\033[1m !!  Ход \x1b[36m синего\x1b[0m игрока ! !\033[0m \n");
                            // printf - печать
                            // \033[1m - жирный текст
                            //  !!  Ход  - текст
                            // \x1b[36m - голубой цвет
                            //  синего - это слово голубым
                            // \x1b[0m - сброс цвета
                            //  игрока !! - остальной текст
                            // \033[0m - сброс стилей
                            //   - пробел
                            // \n - новая строка
                            // ; конец команды
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант:  левый игрок нажал 'm' (это тоже кнопка правого!)
                        case 'm': 
                        // case 'm' - вариант "буква m"
                        // : начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m!!  Ход \x1b[36m синего\x1b[0m игрока !!\033[0m \n");
                            // Аналогично предыдущему printf
                            // ; конец команды
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант: нажат пробел
                        case ' ':  // если input равен символу пробела
                        // case ' ' - вариант "пробел" (символ пробела в одинарных кавычках)
                        // : начало кода
                        // Комментарий: если пробел
                        
                            // Левый игрок пропускает ход (ничего не делает)
                            Player = 1;
                            // Просто передаём ход правому игроку
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                    }
                    // } закрывающая фигурная скобка - конец switch для левого игрока
                    // Здесь заканчивается обработка всех возможных нажатий левого игрока
                    
                }
                // } закрывающая фигурная скобка - конец блока if (Player == 0)
                // Здесь заканчивается весь код для левого игрока
                
                // Иначе (если Player НЕ равен 0)
                else {             // если Player равен 1 (правый игрок)
                // else - ключевое слово "иначе"
                // { открывающая фигурная скобка - начало блока else
                
                    // ============================================
                    // БЛОК 10: ОБРАБОТКА НАЖАТИЙ ПРАВОГО ИГРОКА
                    // Аналогично левому игроку, но с другими клавишами
                    // ============================================
                    
                    // Проверяем, какую клавишу нажал правый игрок
                    switch (input) { // проверяем символ
                    // switch - "переключатель"
                    // ( открывающая скобка
                    // input - проверяемая переменная
                    // ) закрывающая скобка
                    // { начало switch
                    // Комментарий: ещё раз проверяем какой символ введён
                    
                        // Вариант: правый игрок нажал 'a' (это кнопка левого!)
                        case 'a':  
                        // case 'a' - вариант "буква a"
                        // :  начало кода
                        
                            // Выводим предупреждение (напоминаем, что сейчас ход правого)
                            printf("\033[1m !!  Ход \x1b[32m красного\x1b[0m игрока !!\033[0m \n");
                            // \x1b[32m - зелёный цвет (используется вместо красного)
                            // ; конец команды
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант: правый игрок нажал 'z' (это кнопка левого!)
                        case 'z':  
                        // case 'z' - вариант "буква z"
                        // :  начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m !! Ход \x1b[32m красного\x1b[0m игрока \033[1m ! !\033[0m \n");
                            // ; конец команды
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант: нажата клавиша 'k'
                        case 'k':   // если input равен 'k'
                        // case 'k' - вариант "буква k"
                        // :  начало кода
                        // Комментарий: движение правой ракетки вверх
                        
                            // Двигаем правую ракетку ВВЕРХ
                            YRightRocket -= 1;
                            // -= вычесть из переменной
                            // Уменьшаем номер строки = движение вверх
                            // ; конец команды
                            
                            // Проверяем границу
                            if (YRightRocket <= 0) { // если вышла за верх
                            // if - "если"
                            // ( открывающая скобка условия
                            // YRightRocket <= 0 - "позиция 0 или меньше"
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим на строку 1
                                YRightRocket = 1;
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход левому игроку
                            Player = 0;
                            // 0 означает "ходит левый игрок"
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант: нажата клавиша 'm'
                        case 'm':  // если input равен 'm'
                        // case 'm' - вариант "буква m"
                        // : начало кода
                        // Комментарий: движение левой правой вниз (опечатка в комментарии, должно быть "правой")
                        
                            // Двигаем правую ракетку ВНИЗ
                            YRightRocket += 1;
                            // += прибавить к переменной
                            // Увеличиваем номер строки = движение вниз
                            // ; конец команды
                            
                            // Проверяем границу
                            if (YRightRocket <= 0) { // если вышла за верх (это условие не имеет смысла здесь)
                            // Это условие никогда не будет истинно после += 1
                            // Возможно, опечатка в коде
                            // { начало блока if
                            
                                YRightRocket = 1;
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход левому игроку
                            Player = 0;
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                        // Вариант: нажат пробел
                        case ' ':  // если input равен пробелу
                        // case ' ' - вариант "пробел"
                        // : начало кода
                        // Комментарий: если пробел
                        
                            // Правый игрок пропускает ход
                            Player = 0;
                            // Передаём ход левому игроку
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch
                            break;
                            // ; конец команды
                            
                    }
                    // } закрывающая фигурная скобка - конец switch для правого игрока
                    // Здесь заканчивается обработка всех нажатий правого игрока
                    
                }
                // } закрывающая фигурная скобка - конец блока else
                // Здесь заканчивается весь код для правого игрока
                
                // ============================================
                // БЛОК 11: ОЧИСТКА ЭКРАНА
                // ============================================
                
                // Очищаем весь экран (стираем всё, что было нарисовано)
                printf("\033[2J\033[H");
                // printf - печать
                // "\033[2J - ANSI escape-код:  "очистить весь экран"
                // \033[H - ANSI escape-код: "переместить курсор в левый верхний угол (позиция 0,0)"
                // " закрывающая кавычка
                // ; конец команды
                // Комментарий:  Очищаем консоль

                // ============================================
                // БЛОК 12: ДВИЖЕНИЕ МЯЧА
                // Мяч двигается после каждого хода любого игрока
                // ============================================
                // Комментарий: Координаты для отрисовки мяча XBall, YBall
                
                // Прибавляем скорость к позиции мяча (двигаем мяч)
                XBall += speedX;   // двигаем по горизонтали
                // XBall - позиция мяча по X
                // += оператор "прибавить к переменной"
                // speedX - скорость/направление (1 = вправо, -1 = влево)
                // XBall += speedX это сокращение от XBall = XBall + speedX
                // Если speedX = 1: было XBall = 40, стало XBall = 41 (мяч сдвинулся вправо)
                // Если speedX = -1: было XBall = 40, стало XBall = 39 (мяч сдвинулся влево)
                // ; конец команды
                
                YBall += speedY;   // двигаем по вертикали
                // YBall - позиция мяча по Y
                // += прибавить к переменной
                // speedY - скорость/направление (1 = вниз, -1 = вверх)
                // Если speedY = 1: мяч сдвинется вниз на 1 строку
                // Если speedY = -1: мяч сдвинется вверх на 1 строку
                // ; конец команды

                // ============================================
                // БЛОК 13: ОТСКОК ОТ ЛЕВОЙ РАКЕТКИ
                // Проверяем, не коснулся ли мяч левой ракетки
                // ============================================
                
                // Проверяем:  мяч на расстоянии 1 клетки от левой ракетки? 
                if (XBall == XLeftRocket + 1) { // если мяч в столбце 3 (2 + 1)
                // if - "если"
                // ( открывающая скобка условия
                // XBall == XLeftRocket + 1 - "позиция мяча равна позиции ракетки плюс 1"
                // == проверка на равенство
                // + сложение
                // XLeftRocket = 2, XLeftRocket + 1 = 3
                // Если мяч в столбце 3, значит он касается ракетки (ракетка в столбце 2)
                // ) закрывающая скобка условия
                // { начало блока if
                // Комментарий: мяч коснулся левой ракетки
                
                    // ============================================
                    // Проверяем, КАКОЙ ЧАСТИ ракетки коснулся мяч
                    // Ракетка занимает 3 строки:  YLeftRocket, YLeftRocket+1, YLeftRocket+2
                    // ============================================
                    
                    // Проверяем:  мяч коснулся ВЕРХНЕЙ части ракетки (первая строка)?
                    if (YBall == YLeftRocket) { // если на той же строке, что и верх ракетки
                    // if - "если"
                    // ( открывающая скобка условия
                    // YBall == YLeftRocket - "позиция мяча по Y равна верхней позиции ракетки"
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий: если мяч коснулся верха платформы то меняем направление на противоположное мяч отлетает в обратную сторону
                    
                        // Разворачиваем направление мяча по вертикали
                        if (speedY <= 0) { // если мяч летел вверх или стоял на месте
                        // if - "если"
                        // ( открывающая скобка условия
                        // speedY <= 0 - "скорость по Y меньше или равна 0"
                        // Если speedY = -1 (вверх) или speedY = 0 (стоит на месте)
                        // ) закрывающая скобка условия
                        // { начало блока if
                        
                            // Мяч летел вверх, теперь полетит вниз
                            speedY = 1;
                            // = присваивание
                            // 1 - движение вниз
                            // ; конец команды
                            
                        }
                        // } конец блока if
                        
                        // Иначе (если мяч летел вниз)
                        else {
                        // else - "иначе"
                        // { начало блока else
                        
                            // Разворачиваем направление (вниз → вверх)
                            speedY = -1 * speedY;
                            // -1 * speedY - умножаем на -1, чтобы изменить знак
                            // * оператор умножения
                            // Если было speedY = 1, станет speedY = -1
                            // ; конец команды
                            
                        }
                        // } конец блока else
                        
                        // Разворачиваем направление по горизонтали
                        speedX = -1 * speedX;
                        // Мяч летел влево (-1), теперь полетит вправо (1)
                        // Или наоборот
                        // ; конец команды
                        
                    }
                    // } конец блока if (YBall == YLeftRocket)
                    // Здесь заканчивается обработка касания верхней части ракетки
                    
                    // Проверяем:  мяч коснулся СРЕДНЕЙ части ракетки (вторая строка)?
                    if (YBall == YLeftRocket + 1) { // если на строке YLeftRocket + 1
                    // if - "если"
                    // ( открывающая скобка условия
                    // YLeftRocket + 1 - вторая строка ракетки (средняя часть)
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий:  Если нижней части платформы то меняем по X а по Y оставляем как есть (в комментарии опечатка, должно быть "средней")
                    
                        // Разворачиваем только горизонтальное направление
                        speedX = -1 * speedX;
                        // Мяч отскочит, но направление по вертикали не изменится
                        // ; конец команды
                        
                    }
                    // } конец блока if
                    // Здесь заканчивается обработка касания средней части
                    
                    // Проверяем: мяч коснулся НИЖНЕЙ части ракетки (третья строка)?
                    if (YBall == YLeftRocket + 1) { // (Внимание! Здесь опечатка в коде:  должно быть +2 для третьей строки)
                    // if - "если"
                    // ( открывающая ск
