// ============================================
// Подключаем библиотеку для работы с вводом-выводом
// stdio.h позволяет использовать printf (печать) и getchar (чтение символа)
// ============================================
#include <stdio.h>

// ============================================
// ОБЪЯВЛЕНИЕ ФУНКЦИЙ
// Говорим компилятору, какие функции будут использоваться
// ============================================

// Функция рисования игрового поля
// int перед названием = функция вернёт целое число
int PrintPole(int width,           // ширина поля
              int height,          // высота поля
              int YLeftRocket,     // позиция левой ракетки по вертикали (номер строки)
              int XLeftRocket,     // позиция левой ракетки по горизонтали (номер столбца)
              int YRightRocket,    // позиция правой ракетки по вертикали
              int XRightRocket,    // позиция правой ракетки по горизонтали
              int XBall,           // позиция мяча по горизонтали
              int YBall,           // позиция мяча по вертикали
              int GoalLeft,        // счёт левого (синего) игрока
              int ScoreRight);     // счёт правого (красного) игрока
// ; конец объявления функции

// void = функция ничего не возвращает (просто выполняет действие)
void PrintLeftRocket();      // рисует левую ракетку (синюю)
// ; конец объявления

void PrintRightRocket();     // рисует правую ракетку (красную)
// ; конец объявления

void PrintWeb();             // рисует сетку посередине поля
// ; конец объявления

void PrintLeftBorderAndWeb(int width);  // (не используется в коде)
// ; конец объявления

void PrintRightBorder(int width);       // (не используется в коде)
// ; конец объявления

void LeftTopAngle();         // рисует левый верхний угол рамки ┌
// ; конец объявления

void RightTopAngle();        // рисует правый верхний угол рамки ┐
// ; конец объявления

void LeftBottomAngle();      // рисует левый нижний угол рамки └
// ; конец объявления

void RightBottomAngle();     // рисует правый нижний угол рамки ┘
// ; конец объявления

void HorizontalBorder(int width);       // рисует горизонтальную линию ───
// ; конец объявления

void PrintRightBorderAfterRocket();     // (не используется в коде)
// ; конец объявления

void PrintBottomBorder();    // рисует нижнюю границу поля
// ; конец объявления

void PrintTopBorder();       // рисует верхнюю границу поля
// ; конец объявления

void PrintBall();            // рисует мяч
// ; конец объявления

void PrintVerticalBorder();  // рисует вертикальную границу │
// ; конец объявления

void PrintScore(int ScreenX, int GoalLeft, int GoalRight);  // выводит счёт игры
// ; конец объявления

// ============================================
// ГЛАВНАЯ ФУНКЦИЯ ПРОГРАММЫ
// Программа начинает работу отсюда
// ============================================
int main() {
// int main() - главная функция, возвращает целое число
// { открывающая фигурная скобка - начало тела функции main

    // ============================================
    // ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ
    // Создаём "коробки" для хранения чисел
    // ============================================
    
    // Позиции ракеток по вертикали (номер строки на экране)
    int YLeftRocket = 12,
    // int - тип данных "целое число"
    // YLeftRocket - название переменной (позиция левой ракетки по вертикали)
    // = присваивание значения
    // 12 - начальное значение (середина поля высотой 25 строк)
    // , запятая означает "объявляем ещё одну переменную того же типа"
    
        YRightRocket = 12;
    // YRightRocket - позиция правой ракетки по вертикали
    // = 12 - тоже в середине
    // ; точка с запятой означает "команда закончена"
    // Комментарий объясняет:  координаты платформ по вертикали Y = 12 для обеих, чтобы отобразить посередине
    
    // Позиция мяча
    int XBall = 3, YBall = 13;
    // XBall = 3 - мяч в столбце 3 (у левого края, рядом с левой ракеткой)
    // , запятая
    // YBall = 13 - мяч на строке 13 (чуть ниже середины)
    // ; конец команды
    // Комментарий:  координаты мячика, мяч у левого игрока
    
    // Скорость мяча (направление движения)
    int speedX = 1, speedY = 1;
    // speedX = 1 - мяч движется на 1 клетку вправо за ход
    // speedY = 1 - мяч движется на 1 клетку вниз за ход
    // ; конец команды
    // Комментарий: скорость перемещения мячика (на самом деле это направление)
    
    // Флаг хода (чья очередь ходить)
    int Player = 0;
    // Player - переменная, хранящая номер текущего игрока
    // = 0 означает "ходит левый (синий) игрок"
    // ; конец команды
    // Комментарий:  флаг хода:  0 = ход левого игрока, 1 = ход правого игрока
    
    // Счёт игроков
    int GoalLeft = 0, GoalRight = 0;
    // GoalLeft = 0 - левый игрок забил 0 голов
    // , запятая
    // GoalRight = 0 - правый игрок забил 0 голов
    // ; конец команды
    // Комментарий: количество голов для левого и правого игрока
    
    // Размер игрового поля
    int ScreenX = 80, ScreenY = 25;
    // ScreenX = 80 - ширина поля 80 символов
    // ScreenY = 25 - высота поля 25 строк
    // ; конец команды
    // Комментарий: размер игровой площадки
    
    // Позиции ракеток по горизонтали
    int XLeftRocket = 2, XRightRocket = ScreenX - 3;
    // XLeftRocket = 2 - левая ракетка в столбце 2
    // XRightRocket = ScreenX - 3 - правая ракетка в столбце 77 (80 - 3 = 77)
    // - означает "вычесть"
    // ; конец команды

    // ============================================
    // НАЧАЛЬНАЯ ОТРИСОВКА
    // Выводим счёт и рисуем поле перед началом игры
    // ============================================
    
    // Выводим счёт (0 :  0)
    PrintScore(ScreenX, GoalLeft, GoalRight);
    // PrintScore - название функции
    // ( открывающая скобка списка параметров
    // ScreenX - ширина экрана (нужна для центрирования)
    // , запятая разделяет параметры
    // GoalLeft - счёт левого игрока
    // , запятая
    // GoalRight - счёт правого игрока
    // ) закрывающая скобка списка параметров
    // ; конец команды
    
    // Рисуем игровое поле с начальными позициями
    PrintPole(ScreenX, ScreenY, YLeftRocket, XLeftRocket, YRightRocket, XRightRocket, XBall, YBall, GoalLeft, GoalRight);
    // PrintPole - функция рисования поля
    // ( открывающая скобка
    // ScreenX, ScreenY - размеры поля
    // YLeftRocket, XLeftRocket - позиция левой ракетки
    // YRightRocket, XRightRocket - позиция правой ракетки
    // XBall, YBall - позиция мяча
    // GoalLeft, GoalRight - счёт
    // ) закрывающая скобка
    // ; конец команды
    // Комментарий: функция рисует игровое поле в начальном состоянии
    
    // Выводим подсказку, чей ход
    printf("    \033[1m!!  Ход \x1b[36m синего \x1b[0m игрока !!\033[0m \n");
    // printf - функция печати на экран
    // ( открывающая скобка
    // "    " - 4 пробела для отступа
    // \033[1m - ANSI код "жирный текст"
    // !!  Ход  - текст
    // \x1b[36m - ANSI код "голубой цвет"
    // синего  - текст голубым цветом
    // \x1b[0m - сброс форматирования
    //  игрока !! - остальной текст
    // \033[0m - сброс форматирования (дублирование для надёжности)
    // \n - переход на новую строку
    // ) закрывающая скобка
    // ; конец команды

    // Переменная для хранения нажатой клавиши
    char input;
    // char - тип данных "символ" (одна буква, цифра или знак)
    // input - название переменной
    // ; конец команды
    
    // ============================================
    // ГЛАВНЫЙ ИГРОВОЙ ЦИКЛ
    // Бесконечный цикл - игра идёт до нажатия 'q' или достижения 21 очка
    // ============================================
    while (1) {
    // while - ключевое слово "пока"
    // ( открывающая скобка условия
    // 1 - условие (1 = истина, поэтому цикл бесконечный)
    // ) закрывающая скобка условия
    // { открывающая фигурная скобка - начало тела цикла
    // Комментарий:  Запускаем бесконечный цикл
    
        // Ждём нажатия клавиши
        input = getchar();
        // input - переменная для хранения символа
        // = присваивание
        // getchar() - функция чтения одного символа с клавиатуры
        // () пустые скобки - функция без параметров
        // ; конец команды
        // Комментарий: ожидаем ввод с клавиатуры, считываем символ

        // ============================================
        // ПРОВЕРКА ГОЛОВ
        // Проверяем, не ушёл ли мяч за границы поля
        // ============================================
        
        // Если мяч ушёл за левую границу (столбец 0 или меньше)
        if (XBall <= 0) {
        // if - "если"
        // ( открывающая скобка условия
        // XBall <= 0 - "позиция мяча по X меньше или равна 0"
        // <= означает "меньше или равно"
        // ) закрывающая скобка условия
        // { открывающая фигурная скобка - начало блока if
        // Комментарий: мяч вышел за левую границу, гол синему игроку, сбрасываем все значения на начальные
        
            // Увеличиваем счёт левого игрока на 1
            GoalLeft++;
            // GoalLeft - переменная счёта
            // ++ означает "увеличить на 1" (GoalLeft = GoalLeft + 1)
            // ; конец команды
            // Комментарий: добавляем очко левому игроку
            
            // Выводим сообщение о голе с выравниванием по центру
            printf("%*s%sГол \x1b[36m синему \x1b[0m  игроку\n ", ScreenX/2-17," ", " ");
            // printf - функция печати
            // ( открывающая скобка
            // " начало форматированной строки
            // %*s - спецификатор формата:  вставить строку с динамической шириной
            // %s - вставить строку
            // Гол  - текст
            // \x1b[36m - голубой цвет
            // синему  - текст голубым
            // \x1b[0m - сброс цвета
            //   игроку - остальной текст
            // \n - новая строка
            //   - пробел
            // " конец строки
            // , запятая разделяет параметры
            // ScreenX/2-17 - вычисляем отступ для центрирования
            // / деление
            // - вычитание
            // , запятая
            // " " - строка из одного пробела (для %*s)
            // , запятая
            // " " - строка из одного пробела (для %s)
            // ) закрывающая скобка
            // ; конец команды
            // Комментарий:  Сообщение "Гол"
            
            // Сбрасываем все позиции в начальное состояние
            YLeftRocket = 12;
            // = присваивание
            // 12 - позиция в середине поля
            // ; конец команды
            // Комментарий: положение сверху левой ракетки
            
            YRightRocket = 12;
            // ; конец команды
            // Комментарий: положение сверху правой ракетки
            
            XLeftRocket = 2;
            // ; конец команды
            // Комментарий: положение слева левой ракетки
            
            XRightRocket = ScreenX - 3;
            // ScreenX - 3 = 80 - 3 = 77
            // ; конец команды
            // Комментарий: положение слева правой ракетки
            
            XBall = 3;
            // ; конец команды
            // Комментарий: начальное положение мяча в центре левой ракетки
            
            YBall = 13;
            // ; конец команды
            // Комментарий:  начальное положение мяча в центре левой ракетки
            
            speedX = 1;
            // 1 означает "двигаться вправо"
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси X
            
            speedY = 1;
            // 1 означает "двигаться вниз"
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси Y
            
            Player = 0;
            // 0 означает "ходит левый игрок"
            // ; конец команды
            // Комментарий:  Игрок, начинающий ход
            
            continue;
            // continue - команда "пропустить остальной код и начать новый круг цикла"
            // ; конец команды
            // Комментарий: гол левому, новый раунд

        }
        // } закрывающая фигурная скобка - конец блока if
        // Здесь заканчивается обработка гола левому игроку
        
        // Проверяем другое условие (если первое не выполнилось)
        else if (XBall >= ScreenX - 1) {
        // else - "иначе"
        // if - "если"
        // ( открывающая скобка условия
        // XBall >= ScreenX - 1 - "позиция мяча больше или равна 79"
        // >= означает "больше или равно"
        // ) закрывающая скобка условия
        // { открывающая фигурная скобка - начало блока else if
        
            // Увеличиваем счёт правого игрока
            GoalRight++;
            // ++ увеличить на 1
            // ; конец команды
            // Комментарий: добавляем очко правому игроку
            
            // Выводим сообщение о голе
            printf("%*s%sГол \x1b[32m красному \x1b[0m  игроку\n ", ScreenX/2-17," ", " ");
            // \x1b[32m - зелёный цвет (используется как красный)
            // ; конец команды
            // Комментарий: Сообщение "Гол"
            
            // Сбрасываем позиции
            YLeftRocket = 12;
            // ; конец команды
            // Комментарий: положение сверху левой ракетки
            
            YRightRocket = 12;
            // ; конец команды
            // Комментарий: положение сверху правой ракетки
            
            XLeftRocket = 2;
            // ; конец команды
            // Комментарий: положение слева левой ракетки
            
            XRightRocket = ScreenX - 3;
            // ; конец команды
            // Комментарий: положение слева правой ракетки
            
            XBall = XRightRocket - 2;
            // XRightRocket - 2 = 77 - 2 = 75
            // ; конец команды
            // Комментарий: начальное положение мяча в центре правой ракетки
            
            YBall = 13;
            // ; конец команды
            // Комментарий: начальное положение мяча в центре правой ракетки
            
            speedX = -1;
            // -1 означает "двигаться влево"
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси X
            
            speedY = -1;
            // -1 означает "двигаться вверх"
            // ; конец команды
            // Комментарий: начальная скорость и направление движения мяча по оси Y
            
            Player = 1;
            // 1 означает "ходит правый игрок"
            // ; конец команды
            // Комментарий:  Игрок, начинающий ход
            
            continue;
            // continue - переход к следующей итерации цикла
            // ; конец команды
            // Комментарий: гол правому игроку, начинаем новый раунд
            
        }
        // } закрывающая фигурная скобка - конец блока else if
        // Здесь заканчивается обработка гола правому игроку

        // ============================================
        // ПРОВЕРКА ПОБЕДЫ
        // Проверяем, не набрал ли кто-то 21 очко
        // ============================================
        
        // Если левый игрок набрал 21 очко
        if (GoalLeft == 21) {
        // ( открывающая скобка условия
        // GoalLeft == 21 - "счёт левого игрока равен 21"
        // == означает "равно" (проверка на равенство, не путать с = присваиванием)
        // ) закрывающая скобка условия
        // { начало блока if
        // Комментарий: проверка на счёт
        
            // Выводим сообщение о победе
            printf("     Синий игрок попедил со счётом \x1b[34m%d\x1b[0m :  \x1b[31m%d\x1b[0m\n", GoalLeft,GoalRight);
            // printf - функция печати
            // ( открывающая скобка
            // "      - начало строки с 5 пробелами
            // Синий игрок победил со счётом  - текст
            // \x1b[34m - синий цвет
            // %d - место для вставки целого числа
            // \x1b[0m - сброс цвета
            //  :   - двоеточие с пробелами
            // \x1b[31m - красный цвет
            // %d - место для второго числа
            // \x1b[0m - сброс цвета
            // \n - новая строка
            // " конец строки
            // , запятая
            // GoalLeft - первое число (подставится вместо первого %d)
            // , запятая
            // GoalRight - второе число (подставится вместо второго %d)
            // ) закрывающая скобка
            // ; конец команды
            
            // Завершаем программу
            return 0;
            // return - команда "вернуть значение и выйти из функции"
            // 0 - код успешного завершения программы
            // ; конец команды
            
        }
        // } конец блока if
        // Здесь заканчивается проверка победы левого игрока
        
        // Если правый игрок набрал 21 очко
        if (GoalRight == 21) {
        // ( открывающая скобка условия
        // GoalRight == 21 - "счёт правого игрока равен 21"
        // ) закрывающая скобка условия
        // { начало блока if
        
            // Выводим сообщение о победе
            printf("     Красный игрок попедил со счётом \x1b[34m%d\x1b[0m : \x1b[31m%d\x1b[0m\n", GoalLeft, GoalRight);
            // Красный игрок победил - текст
            // ; конец команды
            
            // Завершаем программу
            return 0;
            // ; конец команды
            
        }
        // } конец блока if
        // Здесь заканчивается проверка победы правого игрока

        // ============================================
        // ОБРАБОТКА НАЖАТОЙ КЛАВИШИ
        // switch проверяет значение переменной input
        // ============================================
        switch (input) {
        // switch - ключевое слово "переключатель"
        // ( открывающая скобка
        // input - переменная, которую проверяем
        // ) закрывающая скобка
        // { открывающая фигурная скобка - начало тела switch
        // Комментарий: проверяем какой символ введён
        
            // Вариант:  если нажата клавиша 'q'
            case 'q': 
            // case - ключевое слово "вариант"
            // 'q' - значение для проверки (символ в одинарных кавычках)
            // :  двоеточие означает "начало кода для этого варианта"
            // Комментарий: если введена q, заканчиваем игру
            
                // Выводим сообщение о завершении
                printf("Игра закончена");
                // ; конец команды
                
                // Завершаем программу
                return 0;
                // return 0 - выход из main (программа закрывается)
                // ; конец команды
            
            // Вариант:  если нажата клавиша Enter
            case '\n':
            // '\n' - символ переноса строки (клавиша Enter)
            // : начало кода
            // Комментарий:  игнорируем перевод строки, клавиша ENTER
            
                // Ничего не делаем, выходим из switch
                break;
                // break - команда "выйти из switch"
                // ; конец команды
            
            // Вариант: все остальные клавиши
            default: 
            // default - ключевое слово "все остальные случаи"
            // :  начало кода
            // Комментарий: обработка остальных клавиш
            
                // ============================================
                // ПРОВЕРЯЕМ, ЧЕЙ СЕЙЧАС ХОД
                // ============================================
                
                // Если Player равен 0 (ходит левый/синий игрок)
                if (Player == 0) {
                // if - "если"
                // ( открывающая скобка условия
                // Player == 0 - "переменная Player равна 0"
                // == проверка на равенство
                // ) закрывающая скобка условия
                // { открывающая фигурная скобка - начало блока if
                // Комментарий: проверяем чей ход и в зависимости от нажатой клавиши устанавливаем координаты платформы

                    // Проверяем, какую клавишу нажал левый игрок
                    switch (input) {
                    // switch - переключатель
                    // ( открывающая скобка
                    // input - проверяем нажатую клавишу
                    // ) закрывающая скобка
                    // { начало switch
                    // Комментарий: ещё раз проверяем какой символ введён
                    
                        // Вариант: нажата клавиша 'a'
                        case 'a':
                        // :  начало кода
                        // Комментарий: движение левой ракетки вверх
                        
                            // Двигаем левую ракетку вверх
                            YLeftRocket -= 1;
                            // YLeftRocket - позиция ракетки
                            // -= означает "вычесть из переменной"
                            // -= 1 это сокращение от YLeftRocket = YLeftRocket - 1
                            // Уменьшаем номер строки на 1 (движение вверх)
                            // ; конец команды
                            
                            // Проверяем, не вышла ли ракетка за верхнюю границу
                            if (YLeftRocket <= 0) {
                            // ( открывающая скобка условия
                            // YLeftRocket <= 0 - "ракетка на строке 0 или выше"
                            // <= означает "меньше или равно"
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим ракетку на строку 1 (не даём выйти за границу)
                                YLeftRocket = 1;
                                // = присваивание
                                // 1 - минимально возможная позиция
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход правому игроку
                            Player = 1;
                            // 1 означает "теперь ходит правый игрок"
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            // Выходим из switch
                            break;
                            // break - выход из switch
                            // ; конец команды
                        
                        // Вариант:  нажата клавиша 'z'
                        case 'z':
                        // : начало кода
                        // Комментарий: движение левой ракетки вниз
                        
                            // Двигаем левую ракетку вниз
                            YLeftRocket += 1;
                            // += означает "прибавить к переменной"
                            // += 1 это сокращение от YLeftRocket = YLeftRocket + 1
                            // Увеличиваем номер строки на 1 (движение вниз)
                            // ; конец команды
                            
                            // Проверяем, не вышла ли ракетка за нижнюю границу
                            if (YLeftRocket >= ScreenX) {
                            // ( открывающая скобка условия
                            // YLeftRocket >= ScreenX - "ракетка на строке 80 или ниже"
                            // >= означает "больше или равно"
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим ракетку на строку ScreenX-3
                                YLeftRocket = ScreenX - 3;
                                // ScreenX - 3 = 80 - 3 = 77
                                // Ракетка занимает 3 строки, поэтому вычитаем 3
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход правому игроку
                            Player = 1;
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            break;
                            // ; конец команды
                        
                        // Вариант:  левый игрок нажал 'k' (кнопку правого)
                        case 'k': 
                        // : начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m !!  Ход \x1b[36m синего\x1b[0m игрока ! !\033[0m \n");
                            // \033[1m - жирный текст
                            // \x1b[36m - голубой цвет
                            // синего - текст голубым
                            // \x1b[0m - сброс цвета
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант:  левый игрок нажал 'm' (кнопку правого)
                        case 'm':
                        // : начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m!!  Ход \x1b[36m синего\x1b[0m игрока !!\033[0m \n");
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант: нажат пробел
                        case ' ': 
                        // ' ' - символ пробела в одинарных кавычках
                        // : начало кода
                        // Комментарий: если пробел
                        
                            // Левый игрок пропускает ход
                            Player = 1;
                            // Передаём ход правому игроку
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            break;
                            // ; конец команды
                            
                    }
                    // } закрывающая фигурная скобка - конец switch для левого игрока
                    // Здесь заканчивается обработка нажатий левого игрока
                    
                }
                // } закрывающая фигурная скобка - конец блока if (Player == 0)
                // Здесь заканчивается весь код для левого игрока
                
                // Если Player НЕ равен 0 (ходит правый/красный игрок)
                else {
                // else - "иначе"
                // { открывающая фигурная скобка - начало блока else
                
                    // Проверяем, какую клавишу нажал правый игрок
                    switch (input) {
                    // switch - переключатель
                    // ( открывающая скобка
                    // input - проверяем нажатую клавишу
                    // ) закрывающая скобка
                    // { начало switch
                    // Комментарий: ещё раз проверяем какой символ введён
                    
                        // Вариант: правый игрок нажал 'a' (кнопку левого)
                        case 'a': 
                        // : начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m !! Ход \x1b[32m красного\x1b[0m игрока !!\033[0m \n");
                            // \x1b[32m - зелёный цвет (используется как красный)
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант: правый игрок нажал 'z' (кнопку левого)
                        case 'z':
                        // : начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m !! Ход \x1b[32m красного\x1b[0m игрока \033[1m ! !\033[0m \n");
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант: нажата клавиша 'k'
                        case 'k': 
                        // : начало кода
                        // Комментарий: движение правой ракетки вверх
                        
                            // Двигаем правую ракетку вверх
                            YRightRocket -= 1;
                            // -= вычесть из переменной
                            // Уменьшаем номер строки (движение вверх)
                            // ; конец команды
                            
                            // Проверяем границу
                            if (YRightRocket <= 0) {
                            // ( открывающая скобка условия
                            // YRightRocket <= 0 - ракетка на строке 0 или выше
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим на строку 1
                                YRightRocket = 1;
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход левому игроку
                            Player = 0;
                            // 0 означает "ходит левый игрок"
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            break;
                            // ; конец команды
                        
                        // Вариант: нажата клавиша 'm'
                        case 'm': 
                        // : начало кода
                        // Комментарий: движение правой ракетки вниз
                        
                            // Двигаем правую ракетку вниз
                            YRightRocket += 1;
                            // += прибавить к переменной
                            // Увеличиваем номер строки (движение вниз)
                            // ; конец команды
                            
                            // Проверяем границу
                            if (YRightRocket <= 0) {
                            // ( открывающая скобка условия
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                YRightRocket = 1;
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход левому игроку
                            Player = 0;
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            break;
                            // ; конец команды
                        
                        // Вариант: нажат пробел
                        case ' ':
                        // :  начало кода
                        // Комментарий: если пробел
                        
                            // Правый игрок пропускает ход
                            Player = 0;
                            // Передаём ход левому
                            // ; конец команды
                            // Комментарий: передаём ход другому игроку
                            
                            break;
                            // ; конец команды
                            
                    }
                    // } закрывающая фигурная скобка - конец switch для правого игрока
                    // Здесь заканчивается обработка нажатий правого игрока
                    
                }
                // } закрывающая фигурная скобка - конец блока else
                // Здесь заканчивается весь код для правого игрока
                
                // Очищаем экран
                printf("\033[2J\033[H");
                // \033[2J - ANSI код "очистить экран"
                // \033[H - ANSI код "переместить курсор в левый верхний угол"
                // ; конец команды
                // Комментарий:  Очищаем консоль

                // ============================================
                // ДВИЖЕНИЕ МЯЧА
                // Мяч двигается после каждого хода
                // ============================================
                // Комментарий: Координаты для отрисовки мяча XBall, YBall
                
                // Прибавляем скорость к позиции мяча
                XBall += speedX;
                // XBall += speedX это сокращение от XBall = XBall + speedX
                // Если speedX = 1, мяч сдвинется на 1 клетку вправо
                // Если speedX = -1, мяч сдвинется на 1 клетку влево
                // ; конец команды
                
                YBall += speedY;
                // YBall += speedY это сокращение от YBall = YBall + speedY
                // Если speedY = 1, мяч сдвинется на 1 клетку вниз
                // Если speedY = -1, мяч сдвинется на 1 клетку вверх
                // ; конец команды

                // ============================================
                // ОТСКОК ОТ ЛЕВОЙ РАКЕТКИ
                // ============================================
                
                // Проверяем, коснулся ли мяч левой ракетки
                if (XBall == XLeftRocket + 1) {
                // if - "если"
                // ( открывающая скобка условия
                // XBall == XLeftRocket + 1 - "мяч находится на расстоянии 1 клетки от ракетки"
                // == проверка на равенство
                // + сложение
                // ) закрывающая скобка условия
                // { начало блока if
                // Комментарий: мяч коснулся левой ракетки
                
                    // Проверяем, какой части ракетки коснулся мяч
                    
                    // Если мяч коснулся ВЕРХНЕЙ части ракетки (первая строка)
                    if (YBall == YLeftRocket) {
                    // ( открывающая скобка условия
                    // YBall == YLeftRocket - "мяч на той же строке, что и верх ракетки"
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий: если мяч коснулся верха платформы, то меняем направление на противоположное, мяч отлетает в обратную сторону
                    
                        // Разворачиваем направление по вертикали
                        if (speedY <= 0) {
                        // ( открывающая скобка условия
                        // speedY <= 0 - "мяч летит вверх или стоит на месте"
                        // ) закрывающая скобка условия
                        // { начало блока if
                        
                            // Мяч летел вверх, теперь полетит вниз
                            speedY = 1;
                            // = присваивание
                            // 1 означает "движение вниз"
                            // ; конец команды
                            
                        }
                        // } конец блока if
                        
                        else {
                        // else - "иначе"
                        // { начало блока else
                        
                            // Мяч летел вниз, разворачиваем направление
                            speedY = -1 * speedY;
                            // -1 * speedY умножаем на -1, чтобы изменить знак
                            // * означает "умножить"
                            // Если было 1, станет -1
                            // ; конец команды
                            
                        }
                        // } конец блока else
                        
                        // Разворачиваем направление по горизонтали
                        speedX = -1 * speedX;
                        // Мяч летел влево (-1), теперь полетит вправо (1)
                        // ; конец команды
                        
                    }
                    // } конец блока if (YBall == YLeftRocket)
                    
                    // Если мяч коснулся СРЕДНЕЙ части ракетки (вторая строка)
                    if (YBall == YLeftRocket + 1) {
                    // ( открывающая скобка условия
                    // YLeftRocket + 1 - вторая строка ракетки
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий:  Если нижней части платформы, то меняем по X, а по Y оставляем как есть
                    
                        // Разворачиваем только горизонтальное направление
                        speedX = -1 * speedX;
                        // ; конец команды
                        
                    }
                    // } конец блока if
                    
                    // Если мяч коснулся ТРЕТЬЕЙ части ракетки (третья строка)
                    if (YBall == YLeftRocket + 1) {
                    // ( открывающая скобка условия
                    // YLeftRocket + 1 - третья строка ракетки (опечатка в коде, должно быть +2)
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий: Если нижней части платформы, то меняем по X, а по Y оставляем как есть
                    
                        // Разворачиваем горизонтальное направление
                        speedX = -1 * speedX;
                        // ; конец команды
                        
                        // Останавливаем вертикальное движение
                        speedY = 0;
                        // 0 означает "не двигаться по вертикали"
                        // ; конец команды
                        
                    }
                    // } конец блока if
                    
                }
                // } конец блока if (XBall == XLeftRocket + 1)
                // Здесь заканчивается вся обработка касания левой ракетки

                // ============================================
                // ОТСКОК ОТ ПРАВОЙ РАКЕТКИ
                // Аналогично левой ракетке
                // ============================================
                // Комментарий: мяч коснулся правой ракетки
                
                // Проверяем, коснулся ли мяч правой ракетки
                if (XBall == XRightRocket - 1) {
                // ( открывающая скобка условия
                // XRightRocket - 1 - на расстоянии 1 клетки слева от ракетки
                // ) закрывающая скобка условия
                // { начало блока if
                
                    // Если мяч коснулся ВЕРХНЕЙ части ракетки
                    if (YBall == YRightRocket) {
                    // ( открывающая скобка условия
                    // ) закрывающая скобка условия
                    // { начало блока if
                    
                        // Разворачиваем горизонтальное направление
                        speedX = -1 * speedX;
                        // ; конец команды
                        
                        // Разворачиваем вертикальное направление
                        if (speedY <= 0) {
                        // ( открывающая скобка условия
                        // ) закрывающая скобка условия
                        // { начало блока if
                        
                            speedY = 1;
                            // ; конец команды
                            
                        }
                        // } конец блока if
                        
                        else {
                        // { начало блока else
                        
                            speedY = -1 * speedY;
                            // ; конец команды
                            
                        }
                        // } конец блока else
                        
                    }
                    // } конец блока if (YBall == YRightRocket)
                    
                    // Если мяч коснулся СРЕДНЕЙ части ракетки
                    if (YBall == YRightRocket + 1) {
                    // ( открывающая скобка условия
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий: Если нижней части платформы, то меняем по X, а по Y оставляем как есть
                    
                        speedX = -1 * speedX;
                        // ; конец команды
                        
                    }
                    // } конец блока if
                    
                    // Если мяч коснулся ТРЕТЬЕЙ части ракетки
                    if (YBall == YRightRocket + 1) {
                    // ( открывающая скобка условия
                    // ) закрывающая скобка условия
                    // { начало блока if
                    // Комментарий:  Если нижней части платформы, то меняем по X, а по Y оставляем как есть
                    
                        speedX = -1 * speedX;
                        // ; конец команды
                        
                        speedY = 0;
                        // ; конец команды
                        
                    }
                    // } конец блока if
                    
                }
                // } конец блока if (XBall == XRightRocket - 1)
                // Здесь заканчивается вся обработка касания правой ракетки
                
                // ============================================
                // ОТСКОК ОТ ПОТОЛКА И ПОЛА
                // ============================================
                // Комментарий: если мяч коснулся низа поля, меняем направление движения по Y, чтобы он полетел вверх
                
                // Если мяч коснулся пола (нижней границы)
                if (YBall >= ScreenY - 1) {
                // ( открывающая скобка условия
                // YBall >= ScreenY - 1 - "мяч на строке 24 или ниже"
                // >= означает "больше или равно"
                // ) закрывающая скобка условия
                // { начало блока if
                
                    // Разворачиваем вертикальное направление
                    speedY = -1 * speedY;
                    // Мяч летел вниз (1), теперь полетит вверх (-1)
                    // ; конец команды
                    
                }
                // } конец блока if
                // Комментарий: если мяч коснулся верха поля, меняем направление движения по Y, чтобы он полетел вниз
                
                // Если мяч коснулся потолка (верхней границы)
                if (YBall <= 1) {
                // ( открывающая скобка условия
                // YBall <= 1 - "мяч на строке 1 или выше"
                // ) закрывающая скобка условия
                // { начало блока if
                
                    // Разворачиваем вертикальное направление
                    speedY = -1 * speedY;
                    // Мяч летел вверх (-1), теперь полетит вниз (1)
                    // ; конец команды
                    
                }
                // } конец блока if
                // } закомментированная скобка (вероятно, ошибка в коде)
                
                // ============================================
                // ПЕРЕРИСОВЫВАЕМ ЭКРАН
                // ============================================
                // Комментарий: Выводим счёт
                
                // Выводим счёт
                PrintScore(ScreenX, GoalLeft, GoalRight);
                // PrintScore - функция вывода счёта
                // ( открывающая скобка
                // ScreenX - ширина экрана (для центрирования)
                // , запятая
                // GoalLeft - счёт левого игрока
                // , запятая
                // GoalRight - счёт правого игрока
                // ) закрывающая скобка
                // ; конец команды
                
                // Рисуем поле с обновлёнными позициями
                PrintPole(ScreenX, ScreenY, YLeftRocket, XLeftRocket, YRightRocket, XRightRocket, XBall, YBall, GoalLeft, GoalRight);
                // PrintPole - функция рисования поля
                // ( открывающая скобка
                // ScreenX, ScreenY - размеры поля
                // YLeftRocket, XLeftRocket - позиция левой ракетки
                // YRightRocket, XRightRocket - позиция правой ракетки
                // XBall, YBall - позиция мяча
                // GoalLeft, GoalRight - счёт
                // ) закрывающая скобка
                // ; конец команды
                // Комментарий: перерисовываем поле
                
                // Выводим подсказку, чей сейчас ход
                if (Player == 0) {
                // ( открывающая скобка условия
                // Player == 0 - "ходит левый игрок"
                // ) закрывающая скобка условия
                // { начало блока if
                // Комментарий: Выводим подсказку, какой игрок ходит
                
                    // Выводим "Ход синего игрока"
                    printf("    \033[1m!!  Ход \x1b[36m синего \x1b[0m игрока ! !\033[0m \n");
                    // "    " - 4 пробела для отступа
                    // \033[1m - жирный текст
                    // !!  Ход  - текст
                    // \x1b[36m - голубой цвет
                    // синего  - текст голубым
                    // \x1b[0m - сброс цвета
                    //  игрока !! - остальной текст
                    // \033[0m - сброс форматирования
                    // \n - новая строка
                    // ; конец команды
                    
                }
                // } конец блока if
                
                else {
                // else - "иначе"
                // { начало блока else
                
                    // Выводим "Ход красного игрока" (с отступом справа)
                    printf("%*s%s \033[1m !! Ход \x1b[31m красного \x1b[0m игрока \033[1m ! !\033[0m \n", ScreenX / 2, " ", " ");
                    // %*s - вставить строку с динамической шириной
                    // %s - вставить строку
                    //   - пробел
                    // \033[1m - жирный текст
                    //  !! Ход  - текст
                    // \x1b[31m - красный цвет
                    // красного  - текст красным
                    // \x1b[0m - сброс цвета
                    //  игрока  - остальной текст
                    // \033[1m - жирный текст
                    //  !! - текст
                    // \033[0m - сброс форматирования
                    // \n - новая строка
                    // , запятая
                    // ScreenX / 2 - ширина отступа (половина экрана)
                    // , запятая
                    // " " - строка для первого %*s
                    // , запятая
                    // " " - строка для второго %s
                    // ) закрывающая скобка
                    // ; конец команды
                    
                }
                // } конец блока else
                
        }
        // } закрывающая фигурная скобка - конец блока switch
        // Здесь заканчивается вся обработка нажатых клавиш
        
    }
    // } закрывающая фигурная скобка - конец тела цикла while
    // Здесь заканчивается один круг цикла, программа возвращается к началу while
    
    // Эта строка никогда не выполнится (цикл бесконечный)
    return 0;
    // return 0 - возвращаем 0 (успешное завершение)
    // ; конец команды
    
}
// } закрывающая фигурная скобка - конец функции main
// Здесь заканчивается главная функция программы

// ============================================
// ФУНКЦИЯ РИСОВАНИЯ ПОЛЯ
// ============================================
/* 
 * Многострочный комментарий
 * Рисует прямоугольную область заданной шириной width и высотой height
 */
int PrintPole(int width,           // ширина поля
              int height,          // высота поля
              int YLeftRocket,     // позиция левой ракетки по вертикали
              int XLeftRocket,     // позиция левой ракетки по горизонтали
              int YRightRocket,    // позиция правой ракетки по вертикали
              int XRightRocket,    // позиция правой ракетки по горизонтали
              int XBall,           // позиция мяча по горизонтали
              int YBall,           // позиция мяча по вертикали
              int GoalLeft,        // счёт левого игрока
              int ScoreRight) {    // счёт правого игрока
// int PrintPole - функция возвращает целое число
// ( открывающая скобка списка параметров
// int width - первый параметр (ширина)
// , запятая разделяет параметры
// ...  остальные параметры
// ) закрывающая скобка списка параметров
// { открывающая фигурная скобка - начало тела функции

    // Комментарий: Вывод счёта (сам счёт выводится в функции PrintScore в main)

    // Комментарий: рисуем вертикальную границу (имеется в виду верхнюю рамку)
    // Рисуем верхнюю границу поля
    PrintTopBorder(width);
    // PrintTopBorder - функция рисования верхней границы
    // ( открывающая скобка
    // width - передаём ширину поля
    // ) закрывающая скобка
    // ; конец команды

    // Комментарий: рисуем остальное поле, проходим построчно и отрисовываем каждую строчку
    // ============================================
    // ЦИКЛ ПО СТРОКАМ
    // Рисуем каждую строку поля от 1 до 24
    // ============================================
    for (int y = 1; y < height; ++y) {
    // for - цикл со счётчиком
    // ( открывающая скобка параметров цикла
    // int y = 1 - создаём счётчик y, начинаем с 1
    // ; разделитель
    // y < height - условие продолжения (пока y меньше 25)
    // ; разделитель
    // ++y - увеличиваем y на 1 после каждого круга
    // ) закрывающая скобка параметров цикла
    // { открывающая фигурная скобка - начало тела цикла
    
        // Комментарий: рисуем вертикальную черту левой границы
        // Рисуем левую вертикальную границу
        PrintVerticalBorder();
        // PrintVerticalBorder - функция рисования вертикальной линии
        // () пустые скобки - нет параметров
        // ; конец команды
        
        // Комментарий: проходим курсором по всей ширине поля
        // ============================================
        // ЦИКЛ ПО СТОЛБЦАМ
        // Для каждой строки рисуем все столбцы
        // ============================================
        for (int x = 0; x < width - 1; ++x) {
        // for - цикл со счётчиком
        // ( открывающая скобка параметров цикла
        // int x = 0 - создаём счётчик x, начинаем с 0
        // ; разделитель
        // x < width - 1 - условие (пока x меньше 79)
        // ; разделитель
        // ++x - увеличиваем x на 1
        // ) закрывающая скобка параметров цикла
        // { открывающая фигурная скобка - начало тела цикла
        
            // Комментарий: если достигаем середины поля, то рисуем сетку
            // Проверяем, это середина поля? 
            if (x == width / 2) {
            // if - "если"
            // ( открывающая скобка условия
            // x == width / 2 - "x равен половине ширины"
            // == проверка на равенство
            // / деление (80 / 2 = 40)
            // ) закрывающая скобка условия
            // { начало блока if
            
                // Рисуем символ сетки
                PrintWeb();
                // PrintWeb - функция рисования сетки
                // () пустые скобки
