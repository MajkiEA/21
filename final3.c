// ============================================
// Подключаем библиотеку для работы с вводом-выводом
// stdio.h = Standard Input Output header (заголовочный файл стандартного ввода-вывода)
// Благодаря этому мы можем использовать printf (печать) и getchar (чтение клавиши)
// ============================================
#include <stdio.h>

// ============================================
// ОБЪЯВЛЕНИЕ ФУНКЦИЙ
// Это как "оглавление" - говорим компилятору, что эти функции существуют ниже в коде
// Точка с запятой ; в конце означает "объявление функции закончено"
// ============================================

// Функция рисования игрового поля
// int перед названием = функция вернёт целое число
// В круглых скобках () перечислены параметры (данные, которые передаются в функцию)
int printPole(int width, int height, int YLeftRocket, int XLeftRocket, int YRightRocket, int XRightRocket, int XBall, int YBall, int GoalLeft, int ScoreRight);
// ; означает "объявление закончено"

// void = функция ничего не возвращает (просто выполняет действие)
// () пустые скобки = функция не принимает параметров
void PrintLeftRocket();
// ; конец объявления

void PrintRightRocket();
// ; конец объявления

void PrintWeb();
// ; конец объявления

void PrintLeftBorderAndWeb(int width);
// (int width) = функция принимает один параметр - ширину
// ; конец объявления

void PrintRightBorder(int width);
// ; конец объявления

void LeftTopAngle();
// ; конец объявления

void RightTopAngle();
// ; конец объявления

void LeftBottomAngle();
// ; конец объявления

void RightBottomAngle();
// ; конец объявления

void HorizontalBorder(int width);
// ; конец объявления

void PrintRightBorderAfterRocket();
// ; конец объявления

void PrintBottomBorder();
// ; конец объявления

void PrintTopBorder();
// ; конец объявления

void PrintBall();
// ; конец объявления

void PrintVerticalBorder();
// ; конец объявления

// ============================================
// ГЛАВНАЯ ФУНКЦИЯ ПРОГРАММЫ
// Программа всегда начинает работу с функции main
// ============================================
int main() {
// { открывающая фигурная скобка - начало тела функции main
// Всё, что между { и } относится к функции main

    // ============================================
    // ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ
    // int = целое число (integer)
    // = присваивание значения
    // ; точка с запятой означает "команда закончена"
    // ============================================
    
    // Позиции ракеток по вертикали (номер строки)
    int YLeftRocket = 12, YRightRocket = 12;
    // int YLeftRocket = 12 - создаём переменную YLeftRocket и присваиваем ей значение 12
    // , запятая означает "объявляем ещё одну переменную того же типа"
    // YRightRocket = 12 - вторая переменная тоже равна 12
    // ; конец команды
    
    // координаты мячика по горизонтали (X) и вертикали (Y)
    int XBall = 3, YBall = 13;
    // XBall = 3 - мяч в столбце 3
    // YBall = 13 - мяч на строке 13
    // ; конец команды
    
    // скорость перемещения мячика (направление движения)
    int speedX = 1, speedY = 1;
    // speedX = 1 означает "двигаться на 1 клетку вправо"
    // speedY = 1 означает "двигаться на 1 клетку вниз"
    // ; конец команды
    
    // флаг хода:  чья очередь ходить
    int Player = 0;
    // Player = 0 означает "ходит левый игрок"
    // ; конец команды
    
    // количество голов для каждого игрока
    int GoalLeft = 0, GoalRight = 0;
    // GoalLeft = 0 - левый игрок забил 0 голов
    // GoalRight = 0 - правый игрок забил 0 голов
    // ; конец команды
    
    // размер игровой площадки
    int ScreenX = 80, ScreenY = 25;
    // ScreenX = 80 - ширина поля 80 символов
    // ScreenY = 25 - высота поля 25 строк
    // ; конец команды
    
    // позиции ракеток по горизонтали
    int XLeftRocket = 2, XRightRocket = ScreenX - 3;
    // XLeftRocket = 2 - левая ракетка в столбце 2
    // XRightRocket = ScreenX - 3 - правая ракетка в столбце 77 (80-3)
    // - означает "вычесть"
    // ; конец команды
    
    // Вызываем функцию рисования поля с начальными значениями
    printPole(ScreenX, ScreenY, YLeftRocket, XLeftRocket, YRightRocket, XRightRocket, XBall, YBall, GoalLeft, GoalRight);
    // ( начало списка параметров
    // ScreenX, ScreenY...  передаём все переменные в функцию через запятую
    // ) конец списка параметров
    // ; конец команды

    // Переменная для хранения нажатой клавиши
    char input;
    // char = character = символ (буква, цифра, знак)
    // input - название переменной
    // ; конец команды
    
    // Запускаем бесконечный цикл (игра работает до нажатия 'q')
    while (1) {
    // while - ключевое слово "пока"
    // ( открывающая скобка условия
    // 1 - условие (1 = истина = всегда выполняется)
    // ) закрывающая скобка условия
    // { открывающая фигурная скобка - начало тела цикла while
    // Всё между { и } будет повторяться бесконечно
    
        // Ждём нажатия клавиши и сохраняем её в переменную input
        input = getchar();
        // input - переменная, куда сохраняем результат
        // = присваивание
        // getchar() - функция чтения одного символа с клавиатуры
        // () пустые скобки - функция не принимает параметров
        // ; конец команды

        // ============================================
        // ПРОВЕРКА ГОЛОВ
        // Проверяем, не ушёл ли мяч за границы поля
        // ============================================
        
        // Проверяем, ушёл ли мяч за левую границу
        if (XBall <= 0) {
        // if - ключевое слово "если"
        // ( открывающая скобка условия
        // XBall <= 0 - условие:  "XBall меньше или равен 0"
        // <= означает "меньше или равно"
        // ) закрывающая скобка условия
        // { открывающая фигурная скобка - начало блока if
        // Код внутри { } выполнится только если условие истинно
        
            // Увеличиваем счёт левого игрока на 1
            GoalLeft++;
            // GoalLeft - переменная счёта
            // ++ означает "увеличить на 1" (GoalLeft = GoalLeft + 1)
            // ; конец команды
            
            // Выводим сообщение на экран
            printf(" гол левому \n ");
            // printf - функция печати на экран
            // ( начало параметров
            // " открывающая кавычка - начало текстовой строки
            //  гол левому  - текст, который выведется
            // \n - специальный символ "новая строка"
            //   - пробел
            // " закрывающая кавычка - конец текстовой строки
            // ) конец параметров
            // ; конец команды
            
            // Сбрасываем все позиции в начальное состояние
            YLeftRocket = 12;
            // = присваивание
            // 12 - новое значение
            // ; конец команды
            
            YRightRocket = 12;
            // ; конец команды
            
            XLeftRocket = 2;
            // ; конец команды
            
            XRightRocket = ScreenX - 3;
            // ScreenX - 3 - вычисляем 80 - 3 = 77
            // ; конец команды
            
            XBall = 3;
            // ; конец команды
            
            YBall = 13;
            // ; конец команды
            
            speedX = 1;
            // ; конец команды
            
            speedY = 1;
            // ; конец команды
            
            Player = 0;
            // ; конец команды
            
            // Пропускаем весь остальной код и начинаем новый круг цикла
            continue;
            // continue - команда "перейти к следующей итерации цикла"
            // ; конец команды
            
        }
        // } закрывающая фигурная скобка - конец блока if
        // Здесь заканчивается код, который выполняется при XBall <= 0
        
        // Проверяем другое условие (если первое не выполнилось)
        else if (XBall >= ScreenX - 1) {
        // else - "иначе"
        // if - "если"
        // ( открывающая скобка условия
        // XBall >= ScreenX - 1 - "XBall больше или равен 79"
        // >= означает "больше или равно"
        // ) закрывающая скобка условия
        // { открывающая фигурная скобка - начало блока else if
        
            // Увеличиваем счёт правого игрока
            GoalRight++;
            // ++ увеличить на 1
            // ; конец команды
            
            // Выводим сообщение
            printf(" гол правому \n ");
            // ; конец команды
            
            // Сбрасываем позиции
            YLeftRocket = 12;
            // ; конец команды
            
            YRightRocket = 12;
            // ; конец команды
            
            XLeftRocket = 2;
            // ; конец команды
            
            XRightRocket = ScreenX - 3;
            // ; конец команды
            
            XBall = XRightRocket - 2;
            // XRightRocket - 2 = 77 - 2 = 75
            // ; конец команды
            
            YBall = 13;
            // ; конец команды
            
            speedX = -1;
            // -1 означает "двигаться влево"
            // ; конец команды
            
            speedY = -1;
            // -1 означает "двигаться вверх"
            // ; конец команды
            
            Player = 1;
            // 1 означает "ходит правый игрок"
            // ; конец команды
            
            // Переходим к следующему кругу цикла
            continue;
            // ; конец команды
            
        }
        // } закрывающая фигурная скобка - конец блока else if
        // Здесь заканчивается обработка гола правому игроку

        // ============================================
        // ОБРАБОТКА НАЖАТОЙ КЛАВИШИ
        // switch проверяет значение переменной и выполняет соответствующий код
        // ============================================
        switch (input) {
        // switch - ключевое слово "переключатель"
        // ( открывающая скобка
        // input - переменная, значение которой проверяем
        // ) закрывающая скобка
        // { открывающая фигурная скобка - начало тела switch
        
            // Вариант:  если нажата клавиша 'q'
            case 'q': 
            // case - ключевое слово "вариант"
            // 'q' - значение для проверки (символ в одинарных кавычках)
            // :  двоеточие означает "начало кода для этого варианта"
            
                // Выводим сообщение о завершении игры
                printf("Игра закончена");
                // ; конец команды
                
                // Завершаем функцию main (и всю программу)
                return 0;
                // return - команда "вернуть значение и выйти из функции"
                // 0 - код завершения (0 = успешное завершение)
                // ; конец команды
                // После return программа полностью завершается
            
            // Вариант:  если нажата клавиша Enter
            case '\n':
            // '\n' - символ переноса строки (Enter)
            // : начало кода для этого варианта
            
                // Ничего не делаем, выходим из switch
                break;
                // break - команда "выйти из switch"
                // ; конец команды
            
            // Вариант:  все остальные клавиши
            default: 
            // default - ключевое слово "все остальные случаи"
            // :  начало кода для этого варианта
            
                // ============================================
                // ПРОВЕРЯЕМ, ЧЕЙ СЕЙЧАС ХОД
                // ============================================
                
                // Если Player равен 0 (ходит левый игрок)
                if (Player == 0) {
                // if - "если"
                // ( открывающая скобка условия
                // Player == 0 - "Player равен 0"
                // == означает "равно" (проверка на равенство)
                // Важно: == это проверка, а = это присваивание! 
                // ) закрывающая скобка условия
                // { открывающая фигурная скобка - начало блока if
                
                    // ============================================
                    // ХОД ЛЕВОГО ИГРОКА
                    // Проверяем, какую клавишу нажал левый игрок
                    // ============================================
                    switch (input) {
                    // switch - переключатель
                    // ( открывающая скобка
                    // input - проверяем нажатую клавишу
                    // ) закрывающая скобка
                    // { открывающая фигурная скобка - начало switch
                    
                        // Вариант: нажата клавиша 'a'
                        case 'a':
                        // case 'a' - если input равен 'a'
                        // : начало кода
                        
                            // Двигаем левую ракетку вверх
                            YLeftRocket -= 1;
                            // YLeftRocket - переменная позиции ракетки
                            // -= означает "вычесть из переменной"
                            // -= 1 это сокращение от YLeftRocket = YLeftRocket - 1
                            // Уменьшаем номер строки на 1 (движение вверх)
                            // ; конец команды
                            
                            // Проверяем, не вышла ли ракетка за верхнюю границу
                            if (YLeftRocket <= 0) {
                            // if - "если"
                            // ( открывающая скобка условия
                            // YLeftRocket <= 0 - "ракетка на строке 0 или выше"
                            // ) закрывающая скобка условия
                            // { открывающая фигурная скобка - начало блока if
                            
                                // Ставим ракетку на строку 1 (не даём выйти за границу)
                                YLeftRocket = 1;
                                // = присваивание
                                // 1 - минимально возможная позиция
                                // ; конец команды
                                
                            }
                            // } закрывающая фигурная скобка - конец блока if
                            // Здесь заканчивается проверка границы
                            
                            // Передаём ход правому игроку
                            Player = 1;
                            // = присваивание
                            // 1 означает "теперь ходит правый игрок"
                            // ; конец команды
                            
                            // Выходим из switch (прекращаем проверку других case)
                            break;
                            // break - выход из switch
                            // ; конец команды
                        
                        // Вариант: нажата клавиша 'z'
                        case 'z':
                        // :  начало кода
                        
                            // Двигаем левую ракетку вниз
                            YLeftRocket += 1;
                            // += означает "прибавить к переменной"
                            // += 1 это сокращение от YLeftRocket = YLeftRocket + 1
                            // Увеличиваем номер строки на 1 (движение вниз)
                            // ; конец команды
                            
                            // Проверяем, не вышла ли ракетка за нижнюю границу
                            if (YLeftRocket >= ScreenX) {
                            // ( открывающая скобка условия
                            // YLeftRocket >= ScreenX - "ракетка на строке 80 или ниже"
                            // ) закрывающая скобка условия
                            // { начало блока if
                            
                                // Ставим ракетку на строку ScreenX-3
                                YLeftRocket = ScreenX - 3;
                                // ScreenX - 3 = 80 - 3 = 77
                                // Ракетка занимает 3 строки, поэтому вычитаем 3
                                // ; конец команды
                                
                            }
                            // } конец блока if
                            
                            // Передаём ход правому игроку
                            Player = 1;
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант:  левый игрок нажал 'k' (кнопку правого)
                        case 'k':
                        // :  начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m !!  Ход \x1b[36m левого\x1b[0m игрока ! !\033[0m \n");
                            // \033[1m - ANSI код "жирный текст"
                            // \x1b[36m - ANSI код "голубой цвет"
                            // \x1b[0m - ANSI код "сброс форматирования"
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант:  левый игрок нажал 'm' (кнопку правого)
                        case 'm':
                        // : начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m!!  Ход \x1b[36m левого\x1b[0m игрока !!\033[0m \n");
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                        
                        // Вариант: нажат пробел
                        case ' ':
                        // ' ' - символ пробела в одинарных кавычках
                        // : начало кода
                        
                            // Левый игрок пропускает ход
                            Player = 1;
                            // Передаём ход правому
                            // ; конец команды
                            
                            break;
                            // ; конец команды
                            
                    }
                    // } закрывающая фигурная скобка - конец switch для левого игрока
                    // Здесь заканчивается обработка нажатий левого игрока
                    
                }
                // } закрывающая фигурная скобка - конец блока if (Player == 0)
                // Здесь заканчивается весь код для левого игрока
                
                // Если Player НЕ равен 0 (ходит правый игрок)
                else {
                // else - "иначе"
                // { открывающая фигурная скобка - начало блока else
                
                    // ============================================
                    // ХОД ПРАВОГО ИГРОКА
                    // ============================================
                    switch (input) {
                    // switch - переключатель
                    // ( открывающая скобка
                    // input - проверяем нажатую клавишу
                    // ) закрывающая скобка
                    // { открывающая фигурная скобка - начало switch
                    
                        // Вариант: правый игрок нажал 'a' (кнопку левого)
                        case 'a':
                        // :  начало кода
                        
                            // Выводим предупреждение
                            printf("\033[1m !!  Ход \x1b[32m правого\x1b[0m игрока !!\033[0m \n");
                            // \x1b[32m - зелёный цвет
                            // ; конец команды
                            
                         
